# Archives de la vitrine Opale

Chaque dossier `vX.Y.Z/` est un build statique issu d’un état immuable du dépôt.
Ces fichiers sont volontairement conservés dans le site : une publication future
ne doit pas rendre les anciennes versions inutilisables.
